#!/usr/bin/env node
/**
 * Davidskiltech Expert — Backend Setup Script
 * ─────────────────────────────────────────────
 * STEP 1: Create a NEW empty folder on your Desktop called 'dst-backend'
 * STEP 2: Put this file inside that folder
 * STEP 3: Open Terminal in that folder
 * STEP 4: Run: node setup.js
 * STEP 5: Run: node server.js
 * STEP 6: Open: http://localhost:4000/admin
 */

const fs   = require('fs');
const path = require('path');

console.log('
  ╔══════════════════════════════════════════╗');
console.log('  ║  ⚡ Davidskiltech Backend — Auto Setup   ║');
console.log('  ╚══════════════════════════════════════════╝
');

const FILES = {
  "server.js": "/**\n * Davidskiltech Expert — Backend Server\n * Zero dependencies — built-in Node.js modules only\n * Run: node server.js\n */\n\nconst http   = require('http');\nconst fs     = require('fs');\nconst path   = require('path');\nconst url    = require('url');\nconst crypto = require('crypto');\n\nconst PORT      = process.env.PORT || 4000;\nconst DATA_FILE = path.join(__dirname, 'data.json');\n\n// ── Admin credentials ──────────────────────────────────────────\nconst ADMIN_USER = 'david';\nconst ADMIN_PASS = 'davidskiltech2026';\n\n// ── Data helpers ───────────────────────────────────────────────\nfunction readData() {\n  try { return JSON.parse(fs.readFileSync(DATA_FILE, 'utf8')); }\n  catch { return { messages: [], adminSessions: {} }; }\n}\nfunction writeData(d) {\n  fs.writeFileSync(DATA_FILE, JSON.stringify(d, null, 2), 'utf8');\n}\nfunction genId()    { return 'MSG-' + Date.now() + '-' + crypto.randomBytes(3).toString('hex').toUpperCase(); }\nfunction genToken() { return crypto.randomBytes(24).toString('hex'); }\n\n// ── CORS headers ───────────────────────────────────────────────\n// Allow your Vercel frontend to call this backend\nconst CORS = {\n  'Access-Control-Allow-Origin': '*',\n  'Access-Control-Allow-Methods': 'GET,POST,OPTIONS',\n  'Access-Control-Allow-Headers': 'Content-Type,Authorization'\n};\n\nfunction sendJSON(res, code, payload, extra = {}) {\n  const body = JSON.stringify(payload);\n  res.writeHead(code, { 'Content-Type': 'application/json', ...CORS, ...extra });\n  res.end(body);\n}\n\nfunction parseBody(req) {\n  return new Promise((resolve, reject) => {\n    let b = '';\n    req.on('data', c => { b += c; if (b.length > 1024 * 1024) reject(new Error('Too large')); });\n    req.on('end', () => { try { resolve(JSON.parse(b || '{}')); } catch { reject(new Error('Bad JSON')); } });\n    req.on('error', reject);\n  });\n}\n\n// ── Admin session helpers ──────────────────────────────────────\nconst SESSION_TTL = 8 * 3600 * 1000;\n\nfunction genAdminToken() {\n  const t = genToken();\n  const d = readData();\n  if (!d.adminSessions) d.adminSessions = {};\n  // Clean expired sessions\n  for (const [k, v] of Object.entries(d.adminSessions)) {\n    if (Date.now() - v.createdAt > SESSION_TTL) delete d.adminSessions[k];\n  }\n  d.adminSessions[t] = { createdAt: Date.now() };\n  writeData(d);\n  return t;\n}\n\nfunction checkAdminSession(req) {\n  const m = (req.headers['cookie'] || '').match(/dst_admin=([a-f0-9]+)/);\n  if (!m) return false;\n  const d = readData();\n  if (!d.adminSessions || !d.adminSessions[m[1]]) return false;\n  if (Date.now() - d.adminSessions[m[1]].createdAt > SESSION_TTL) {\n    delete d.adminSessions[m[1]];\n    writeData(d);\n    return false;\n  }\n  return true;\n}\n\n// ── API: Contact form submission ───────────────────────────────\nasync function handleContact(req, res) {\n  let b;\n  try { b = await parseBody(req); } catch { return sendJSON(res, 400, { error: 'Invalid request' }); }\n\n  const { name, email, phone, message } = b;\n  if (!name || !email || !message)\n    return sendJSON(res, 400, { error: 'Name, email and message are required.' });\n\n  const d = readData();\n  if (!d.messages) d.messages = [];\n\n  const id = genId();\n  d.messages.push({\n    id, name: name.trim(), email: email.trim(),\n    phone: (phone || '').trim(), message: message.trim(),\n    status: 'Unread', receivedAt: new Date().toISOString()\n  });\n  writeData(d);\n  console.log(`[CONTACT] ${id} from ${name} <${email}>`);\n  sendJSON(res, 200, { success: true, message: `Thank you ${name.trim()}! We'll be in touch shortly.` });\n}\n\n// ── Admin: Login page ──────────────────────────────────────────\nfunction handleAdminLoginPage(req, res) {\n  if (checkAdminSession(req)) { res.writeHead(302, { Location: '/admin' }); return res.end(); }\n  const html = `<!DOCTYPE html><html lang=\"en\"><head><meta charset=\"UTF-8\"><meta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\n<title>Davidskiltech Admin</title>\n<style>\n*{margin:0;padding:0;box-sizing:border-box}\nbody{font-family:system-ui,sans-serif;background:#080c0a;display:flex;align-items:center;justify-content:center;min-height:100vh;padding:20px}\n.card{background:#0f1612;border:1px solid rgba(0,255,136,0.15);border-radius:16px;width:100%;max-width:400px;padding:44px;box-shadow:0 30px 80px rgba(0,0,0,.6)}\n.logo{font-size:20px;font-weight:800;color:#fff;text-align:center;margin-bottom:8px}\n.logo span{color:#00FF88}\n.sub{font-size:13px;color:#7d9489;text-align:center;margin-bottom:28px}\n.badge{background:rgba(0,255,136,0.06);border:1px solid rgba(0,255,136,0.2);border-radius:8px;padding:10px 14px;font-size:13px;color:#00FF88;text-align:center;margin-bottom:20px}\nlabel{display:block;font-size:12px;font-weight:600;letter-spacing:.1em;text-transform:uppercase;color:#7d9489;margin-bottom:6px}\ninput{width:100%;padding:13px 16px;background:#0d1210;border:1.5px solid rgba(0,255,136,0.15);border-radius:8px;font-size:15px;color:#fff;outline:none;transition:border-color .2s;margin-bottom:16px}\ninput:focus{border-color:#00FF88;box-shadow:0 0 0 3px rgba(0,255,136,0.08)}\nbutton{width:100%;padding:14px;background:#00FF88;color:#000;border:none;border-radius:8px;font-size:15px;font-weight:700;cursor:pointer;transition:all .2s;margin-top:4px}\nbutton:hover{background:#fff;transform:translateY(-2px)}\nbutton:disabled{opacity:.6;cursor:not-allowed;transform:none}\n.err{background:rgba(239,68,68,.1);color:#ef4444;border:1px solid rgba(239,68,68,.3);border-radius:8px;padding:12px;font-size:14px;margin-bottom:16px;display:none}\n.err.show{display:block}\n</style></head><body>\n<div class=\"card\">\n  <div class=\"logo\">Davidskil<span>tech</span> Expert</div>\n  <div class=\"sub\">Admin Dashboard Access</div>\n  <div class=\"badge\">🔒 Restricted — Authorized Personnel Only</div>\n  <div class=\"err\" id=\"err\">Wrong username or password.</div>\n  <form id=\"form\">\n    <label>Username</label>\n    <input type=\"text\" id=\"u\" placeholder=\"Admin username\" required autofocus>\n    <label>Password</label>\n    <input type=\"password\" id=\"p\" placeholder=\"Admin password\" required>\n    <button type=\"submit\" id=\"btn\">Sign In →</button>\n  </form>\n</div>\n<script>\ndocument.getElementById('form').addEventListener('submit', async e => {\n  e.preventDefault();\n  const btn = document.getElementById('btn');\n  btn.disabled = true; btn.textContent = 'Verifying...';\n  const r = await fetch('/api/admin/login', {\n    method: 'POST',\n    headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({ username: document.getElementById('u').value, password: document.getElementById('p').value })\n  });\n  if (r.ok) { window.location.href = '/admin'; }\n  else { document.getElementById('err').classList.add('show'); btn.disabled = false; btn.textContent = 'Sign In →'; }\n});\n</script>\n</body></html>`;\n  res.writeHead(200, { 'Content-Type': 'text/html' }); res.end(html);\n}\n\n// ── Admin: Login POST ──────────────────────────────────────────\nasync function handleAdminLoginPost(req, res) {\n  let b; try { b = await parseBody(req); } catch { return sendJSON(res, 400, { error: 'Bad request' }); }\n  if (b.username !== ADMIN_USER || b.password !== ADMIN_PASS) {\n    console.log(`[ADMIN] ❌ Failed login — \"${b.username}\"`);\n    return sendJSON(res, 401, { error: 'Wrong credentials' });\n  }\n  const t = genAdminToken();\n  console.log('[ADMIN] ✅ Logged in');\n  sendJSON(res, 200, { ok: true }, { 'Set-Cookie': `dst_admin=${t}; Path=/; HttpOnly; Max-Age=${8*3600}` });\n}\n\n// ── Admin: Logout ──────────────────────────────────────────────\nfunction handleAdminLogout(req, res) {\n  const m = (req.headers['cookie'] || '').match(/dst_admin=([a-f0-9]+)/);\n  if (m) {\n    const d = readData();\n    if (d.adminSessions) delete d.adminSessions[m[1]];\n    writeData(d);\n  }\n  res.writeHead(302, { Location: '/admin/login', 'Set-Cookie': 'dst_admin=; Path=/; Expires=Thu, 01 Jan 1970 00:00:00 GMT' });\n  res.end();\n}\n\n// ── Admin: Dashboard ───────────────────────────────────────────\nfunction handleAdmin(req, res) {\n  if (!checkAdminSession(req)) { res.writeHead(302, { Location: '/admin/login' }); return res.end(); }\n  const d = readData();\n  const msgs = (d.messages || []).slice().reverse();\n  const unread = msgs.filter(m => m.status === 'Unread').length;\n\n  const html = `<!DOCTYPE html><html lang=\"en\"><head><meta charset=\"UTF-8\"><meta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\n<title>Davidskiltech Admin</title>\n<style>\n*{margin:0;padding:0;box-sizing:border-box}\nbody{font-family:system-ui,sans-serif;background:#080c0a;color:#e8f0eb;padding:24px}\n.top{display:flex;align-items:center;justify-content:space-between;margin-bottom:4px;flex-wrap:wrap;gap:12px}\nh1{color:#00FF88;font-size:20px}\n.logout{background:#0f1612;border:1px solid #ef4444;color:#ef4444;padding:8px 16px;border-radius:8px;cursor:pointer;font-size:13px;font-weight:600;transition:all .2s;text-decoration:none}\n.logout:hover{background:#ef4444;color:#fff}\n.meta{color:#7d9489;font-size:13px;margin-bottom:28px}\n.stats{display:flex;gap:16px;flex-wrap:wrap;margin-bottom:32px}\n.stat{background:#0f1612;border:1px solid rgba(0,255,136,0.15);border-radius:10px;padding:18px 26px}\n.stat-num{font-size:28px;font-weight:700;color:#00FF88}\n.stat-label{font-size:11px;color:#7d9489;margin-top:4px;text-transform:uppercase;letter-spacing:1px}\nh2{color:#00FF88;font-size:15px;margin:28px 0 12px}\n.table-wrap{overflow-x:auto}\ntable{border-collapse:collapse;width:100%;font-size:13px;min-width:600px}\nth{background:#0d1210;color:#00FF88;padding:10px 14px;text-align:left;white-space:nowrap;border-bottom:1px solid rgba(0,255,136,0.15)}\ntd{padding:10px 14px;border-bottom:1px solid rgba(0,255,136,0.06);vertical-align:top;max-width:280px;word-break:break-word}\ntr:hover td{background:#0d1210}\n.badge-unread{display:inline-block;padding:2px 10px;border-radius:4px;font-size:11px;background:rgba(0,255,136,0.15);color:#00FF88;border:1px solid rgba(0,255,136,0.3)}\n.badge-read{display:inline-block;padding:2px 10px;border-radius:4px;font-size:11px;background:#1a1a1a;color:#7d9489}\n.empty{text-align:center;color:#7d9489;padding:40px}\na{color:#00FF88}\n</style></head><body>\n<div class=\"top\">\n  <h1>⚡ Davidskiltech Expert — Admin Dashboard</h1>\n  <a href=\"/admin/logout\" class=\"logout\" onclick=\"return confirm('Sign out?')\">🚪 Sign Out</a>\n</div>\n<p class=\"meta\">Last refreshed: ${new Date().toLocaleString('en-NG')} &nbsp;|&nbsp; <a href=\"/admin\">↻ Refresh</a></p>\n\n<div class=\"stats\">\n  <div class=\"stat\"><div class=\"stat-num\">${msgs.length}</div><div class=\"stat-label\">Total Messages</div></div>\n  <div class=\"stat\"><div class=\"stat-num\" style=\"color:#f97316\">${unread}</div><div class=\"stat-label\">Unread</div></div>\n  <div class=\"stat\"><div class=\"stat-num\">${msgs.length - unread}</div><div class=\"stat-label\">Read</div></div>\n</div>\n\n<h2>📬 Contact Messages (${msgs.length})</h2>\n<div class=\"table-wrap\">\n<table>\n  <tr><th>#</th><th>ID</th><th>Name</th><th>Email</th><th>Phone</th><th>Message</th><th>Received</th><th>Status</th></tr>\n  ${msgs.map((m, i) => `\n  <tr>\n    <td>${i+1}</td>\n    <td style=\"font-size:10px\">${m.id}</td>\n    <td>${m.name}</td>\n    <td><a href=\"mailto:${m.email}\">${m.email}</a></td>\n    <td>${m.phone || '—'}</td>\n    <td>${(m.message || '').slice(0, 80)}${m.message && m.message.length > 80 ? '…' : ''}</td>\n    <td style=\"font-size:11px;white-space:nowrap\">${new Date(m.receivedAt).toLocaleString('en-NG')}</td>\n    <td><span class=\"${m.status === 'Unread' ? 'badge-unread' : 'badge-read'}\">${m.status}</span></td>\n  </tr>`).join('') || `<tr><td colspan=\"8\" class=\"empty\">No messages yet</td></tr>`}\n</table>\n</div>\n</body></html>`;\n  res.writeHead(200, { 'Content-Type': 'text/html' }); res.end(html);\n}\n\n// ── Health check ───────────────────────────────────────────────\nfunction handleHealth(req, res) {\n  sendJSON(res, 200, { status: 'ok', service: 'Davidskiltech Backend', messages: readData().messages?.length || 0 });\n}\n\n// ── Router ─────────────────────────────────────────────────────\nconst server = http.createServer(async (req, res) => {\n  const { pathname } = url.parse(req.url);\n  const method = req.method.toUpperCase();\n\n  // Handle CORS preflight\n  if (method === 'OPTIONS') {\n    res.writeHead(204, CORS); return res.end();\n  }\n\n  console.log(`${method} ${pathname}`);\n\n  if (pathname === '/api/contact'    && method === 'POST') return handleContact(req, res);\n  if (pathname === '/api/health'     && method === 'GET')  return handleHealth(req, res);\n  if (pathname === '/api/admin/login'&& method === 'POST') return handleAdminLoginPost(req, res);\n  if (pathname === '/admin/login'    && method === 'GET')  return handleAdminLoginPage(req, res);\n  if (pathname === '/admin/logout'   && method === 'GET')  return handleAdminLogout(req, res);\n  if (pathname === '/admin'          && method === 'GET')  return handleAdmin(req, res);\n\n  // 404\n  res.writeHead(404, { 'Content-Type': 'application/json', ...CORS });\n  res.end(JSON.stringify({ error: 'Not found' }));\n});\n\nserver.listen(PORT, () => {\n  console.log('\\n  ╔══════════════════════════════════════════╗');\n  console.log('  ║  ⚡ Davidskiltech Backend — Started!     ║');\n  console.log('  ╚══════════════════════════════════════════╝');\n  console.log(`\\n  🌐  API      : http://localhost:${PORT}/api/contact`);\n  console.log(`  📊  Admin    : http://localhost:${PORT}/admin`);\n  console.log(`  💚  Health   : http://localhost:${PORT}/api/health`);\n  console.log('\\n  Press Ctrl+C to stop.\\n');\n});\n",
  "package.json": "{\n  \"name\": \"davidskiltech-backend\",\n  \"version\": \"1.0.0\",\n  \"main\": \"server.js\",\n  \"scripts\": {\n    \"start\": \"node server.js\"\n  }\n}"
};

let ok = 0, fail = 0;
for (const [name, content] of Object.entries(FILES)) {
  try {
    fs.writeFileSync(path.join(__dirname, name), content, 'utf8');
    console.log('  ✅  ' + name + ' — created');
    ok++;
  } catch(e) {
    console.log('  ❌  ' + name + ' — FAILED: ' + e.message);
    fail++;
  }
}

console.log('
  ─────────────────────────────────────────');
if (fail === 0) {
  console.log('  ✅  All ' + ok + ' files created!
');
  console.log('  👉  Now run:
');
  console.log('        node server.js
');
  console.log('  💚  API Health : http://localhost:4000/api/health');
  console.log('  📊  Admin      : http://localhost:4000/admin');
  console.log('      Username   : david');
  console.log('      Password   : davidskiltech2026
');
} else {
  console.log('  ⚠️  ' + fail + ' file(s) failed. Try: sudo node setup.js
');
}
